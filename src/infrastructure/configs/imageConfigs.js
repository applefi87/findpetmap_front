const articleImage = {
  maxAmount: 4,
  maxFileSize: 5
};
const normalImage = {
  quality: 80,
  width: 1080,
  height: 1080
};
const previewImage = {
  quality: 60,
  width: 100,
  height: 100
}

export default { articleImage, normalImage, previewImage };
